import { createTheme } from "@mui/material"

export const theme = createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: "#665C64",
    },
    success: {
      main: "#295FCC",
    },
    warning: {
      main: "#CC9629",
    },
  },
})
